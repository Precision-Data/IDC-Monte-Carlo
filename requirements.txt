# Pinned dependencies for reproducibility of the IDC-Monte-Carlo simulation.
# Versions chosen to match the lower bounds in pyproject.toml and the
# Section 5.2 declaration in the analysis plan.
# Update only via a deviation-logged version bump; CI verifies that the
# canonical simulation output remains stable under this exact set.

numpy==2.3.4
scipy==1.15.3
pandas==2.3.3
matplotlib==3.10.6
pyyaml==6.0.3
pyarrow==20.0.0

# Test / dev (kept here so CI installs from a single file)
pytest==8.4.2
pytest-cov==6.2.1
