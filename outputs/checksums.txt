# Blessed checksum for the canonical principal-seed simulation output.
#
# Format: '<sha256>  <filename>' (one entry per line, '#' lines ignored).
#
# The CI workflow (.github/workflows/reproduce.yml) verifies that a clean
# build reproduces this exact value. Any divergence fails the build.
#
# IMPORTANT: For .parquet files the recorded SHA256 is computed over a
# canonical CSV serialisation of the DataFrame (sorted by
# (prior_set, error_type, sample_index, horizon), float values formatted
# with %.15g) -- NOT over the raw Parquet bytes. This makes the
# reproducibility check survive platform-dependent Parquet metadata,
# dictionary encoding, and compression variation, while still pinning
# every numeric output value bit-for-bit. See
# `idc_simulation.run_log.sha256_dataframe_content` for the exact
# canonicalisation.
#
# Generated with the plan defaults:
#   K = 10000, seed = 20260502, plan_version = 1.1,
#   priors = {optimistic, moderate, pessimistic}.
# Local reproducer (Python 3.13.12 on macOS arm64): confirmed identical
# across two consecutive runs on 2026-05-02.

8dfa2ba116dfbd846cddd04ad04dcb89108642d45ac09ec93079059540acd532  contamination_seed20260502_K10000.parquet
